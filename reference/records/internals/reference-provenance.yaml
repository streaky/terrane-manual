format: 1
id: internals.semantics.reference-provenance
kind: topic

surface: null

lifecycle:
  status: current
  stability: experimental
  since: "0.1"
  deprecated-since: null
  removed-in: null
  replacement: null

documentation:
  status: complete
  title: Reference provenance and native lowering
  title-style: text
  summary: >-
    Records the compiler-owned reference path proof and representation selection.
  tags: [compiler, semantics, references, provenance, lowering]
  sections:
    - id: representation
      title: Semantic representation
      blocks:
        - type: markdown
          markdown: |-
            `ReferenceProvenance` records the owner span, selected external-lender parameter
            span, ordered `Field`, `Element`, and `CallResult` projections, and the first
            lifetime-ending source span. Analysis copies this record through parentheses,
            reference bindings, captures, borrowed iteration, and selected-lender calls; it adds
            projections for member access, supported list/map/unordered-map indexing, returned
            call results, and borrowed iteration targets. Reference-return lender contracts are
            inferred from return flow to a fixed point through calls rather than from arity.
    - id: rejection
      title: Escape and invalidation
      blocks:
        - type: markdown
          markdown: |-
            Local-owner and by-value-parameter return, ambiguous lender calls, unsupported index
            owners, shared-owner promotion, and any observer use after invalidating mutation,
            movement, or replacement are rejected before lowering. Async liveness and capture
            checks consume the same provenance and require the selected lender proof to remain
            valid at those boundaries.
    - id: lowering
      title: Native representation
      blocks:
        - type: markdown
          markdown: |-
            A proven bounded `ref T` is emitted as a Rust `&T`; selected return lenders produce
            explicit function/result lifetimes, field and supported collection-element derivation
            borrows the stored value directly, captured refs remain borrows, and borrowed list
            iteration yields `&T`. Only explicit `shared ref T` storage uses `Arc<Mutex<T>>`.
            When an ordinary ref observes that same explicitly shared owner, it remains non-owning
            through a `Weak` handle. No ordinary reference is silently promoted to ownership.
            Reference-address lowering records an internal `S9005` diagnostic and aborts
            compilation if a semantically admitted provenance shape has no native lowering arm.
            The compiler does not panic or pass a partial generated program to Cargo when semantic
            and lowering support drift apart.
  see-also:
    - lang.types.references

provenance:
  specification:
    - document: language-spec
      section: "12.4"
  conformance:
    - references-derived-provenance
    - reference-return-selected-lender
    - reference-group-map
    - reference-call-result-invalidation
    - borrow-across-await
  compiler: null
