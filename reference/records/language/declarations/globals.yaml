format: 1
id: lang.declarations.globals
kind: topic

surface: null

lifecycle:
  status: current
  stability: experimental
  since: "0.1"
  deprecated-since: null
  removed-in: null
  replacement: null

documentation:
  status: complete
  title: "Program global initialization graph"
  title-style: text
  summary: >-
    Program-global initialization uses an explicit dependency graph. Dependency cycles are diagnosed at compile time.
  tags:
    - "names"
    - "globals"
    - "global-initialization"
  sections:
    - id: global-initialization
      title: "Program-Global Initialization"
      blocks:
        - type: markdown
          markdown: |-
            Program globals form an explicit initialization dependency graph. Dependencies
            between global initializers determine the required initialization relationships.

            A dependency cycle in this graph is invalid and is diagnosed during compilation.
            Cyclic globals are not assigned an arbitrary initialization order.

            Initialization ordering does not by itself make mutable global state safe to
            share. A mutable global used across threads must satisfy the shared-thread-safe
            protocol. State that belongs to one thread should use the standard thread-local
            object rather than a separate global grammar.
  see-also: []

provenance:
  specification:
    - document: language-scoreboard
      section: "global-initialization"
  conformance: null
  compiler: null
